services:
  storage:
    image: redis
    volumes:
      - ./data:/data
  books:
    image: node:20.10
    working_dir: /app
    volumes:
      - ./:/app
    environment:
      - PORT=3002
      - REDIS_URL=redis://storage
    ports:
      - 80:3002
    command: npm run dev
    depends_on:
      - storage